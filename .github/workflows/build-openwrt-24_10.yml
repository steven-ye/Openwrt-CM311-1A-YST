name: Build OpenWrt 24.10 for CM311-1a

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: 'Release 标签（如 v24.10-cm311-1a-20260306）'
        required: true
        default: 'v24.10-cm311-1a'
      release_name:
        description: 'Release 显示名称'
        required: true
        default: 'OpenWrt 24.10 for CM311-1a (BLE HID 支持)'

permissions:
  contents: write  # 必须开启，用于创建/上传 Release

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      # 1. 提取 OpenWrt 主版本
      - name: Extract main version
        id: version
        run: |
          FULL_VERSION="24.10.5"
          MAIN_VERSION=$(echo "$FULL_VERSION" | awk -F '.' '{print $1"."$2}')
          echo "main_version=$MAIN_VERSION" >> $GITHUB_OUTPUT

      # 2. 环境准备（适配 Ubuntu 24.04）
      - name: Prepare build environment
        run: |
          sudo apt update && sudo apt upgrade -y
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
          gettext git libncurses5-dev libssl-dev rsync unzip zlib1g-dev file wget \
          libelf-dev liblzma-dev swig python3-pip patchelf device-tree-compiler \
          python3-setuptools python3-dev libffi-dev pkg-config python3-venv
          sudo ln -sf /usr/bin/python3 /usr/bin/python
          sudo pip3 install setuptools==65.5.0 wheel

      # 3. 克隆源码 + 修复 CM311-1a 补丁路径
      - name: Clone OpenWrt & Fix Patch
        run: |
          git clone -b openwrt-${{ steps.version.outputs.main_version }} --single-branch https://github.com/openwrt/openwrt.git openwrt
          cd openwrt
          PATCH_DIR="target/linux/amlogic/patches-6.6"
          mkdir -p $PATCH_DIR
          wget -O $PATCH_DIR/999-cm311-1a-s905l3a.patch \
          https://raw.githubusercontent.com/ophub/amlogic-s9xxx-openwrt/main/patchs/openwrt-24.10/999-cm311-1a-s905l3a.patch \
          || echo "CM311-1a 补丁下载失败，使用兼容配置"
          ./scripts/feeds update -a
          ./scripts/feeds install -a -f

      # 4. 生成 CM311-1a 配置
      - name: Generate CM311-1a .config
        run: |
          cd openwrt
          make defconfig TARGET=amlogic TARGET_SUBTARGET=s905x3
          cat >> .config << EOF
          CONFIG_TARGET_amlogic=y
          CONFIG_TARGET_amlogic_s905x3=y
          CONFIG_TARGET_amlogic_s905x3_DEVICE_phicomm-n1=y
          CONFIG_KMOD_AMLOGIC_BT=y
          CONFIG_KMOD_BTSDIO=y
          CONFIG_BT=y
          CONFIG_BT_LE=y
          CONFIG_BT_HIDP=y
          CONFIG_KMOD_BLUETOOTH_HID=y
          CONFIG_PACKAGE_bluez-daemon=y
          CONFIG_PACKAGE_bluez-utils=y
          CONFIG_PACKAGE_luci=y
          CONFIG_USE_MUSL=y
          EOF
          make defconfig

      # 5. 编译固件
      - name: Build OpenWrt firmware
        run: |
          cd openwrt
          make -j$(nproc) download V=s || make -j1 download V=s
          make -j$(($(nproc)-1)) V=s || make -j1 V=s

      # 6. 固件重命名（添加日期，方便识别）
      - name: Rename firmware
        id: package
        run: |
          cd openwrt/bin/targets/amlogic/s905x3/
          # 重命名固件文件
          FIRMWARE_OLD=$(ls *.img.gz)
          FIRMWARE_NEW="OpenWrt-24.10-CM311-1a-$(date +%Y%m%d).img.gz"
          mv $FIRMWARE_OLD $FIRMWARE_NEW
          # 输出固件路径和名称
          echo "firmware_path=$(pwd)/$FIRMWARE_NEW" >> $GITHUB_OUTPUT
          echo "firmware_name=$FIRMWARE_NEW" >> $GITHUB_OUTPUT
          # 验证固件存在
          echo "固件文件：$FIRMWARE_NEW"
          ls -l $FIRMWARE_NEW

      # 7. 上传固件到 GitHub Release（使用 ncipollo/release-action）
      - name: Upload the packaged OpenWrt
        uses: ncipollo/release-action@main
        with:
          tag: ${{ github.event.inputs.release_tag }}          # Release 标签
          name: ${{ github.event.inputs.release_name }}        # Release 显示名称
          body: |                                              # Release 描述
            ## OpenWrt 24.10 适配 CM311-1a
            - 硬件：CM311-1a (S905L3A)
            - 核心功能：支持非标准 BLE 蓝牙鼠标（内置 bluez-daemon/bluez-utils）
            - 内核版本：6.6
            - 编译环境：Ubuntu 24.04
            - 固件名称：${{ steps.package.outputs.firmware_name }}
          artifacts: ${{ steps.package.outputs.firmware_path }} # 固件文件路径
          allowUpdates: true                                   # 允许更新已存在的标签
          draft: false                                         # 非草稿
          prerelease: false                                    # 非预发布
          token: ${{ secrets.GITHUB_TOKEN }}                   # GitHub 自动生成的令牌